version: '3.8'

services:
  app:
    container_name: nestjs-app-award
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: npm run start:dev
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - '3001:3001'
    environment:
      NODE_ENV: development
      DATABASE_URL: ${DATABASE_URL}
    depends_on:
      - db

  db:
    container_name: dev-db-award
    image: postgres:17
    restart: unless-stopped
    ports:
      - '${POSTGRES_PORT}:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
